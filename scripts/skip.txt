# Packages / tools the weekly-update orchestrator deliberately skips.
# One entry per line. Comments (#) and blank lines allowed.
# Match is exact package name (npm/bun/uv tool) or exact tool name (rustup component).

# System-managed — apt upgrades, never touch via pip/npm/bun
apt
dpkg

# Not installed on this host — probing would just add noise
pwsh
brew
skills                                                     # skillhub CLI is not installed
pip                                                        # use uv instead

# Version-pinned by another mechanism (e.g. fnm-managed node)
node                                                       # managed by fnm; updates via fnm, not npm